/*
********************************
        移位实现流水灯
********************************
*/
//实验室端口为P0口
#include<reg52.h> //头文件

void delay(int t) //定义延时函数
{
	int y;
	for(;t>0;t--)
	for(y=0;y<200;y++);
}

void main(void) //主函数
{
	while(1)
	{
		int i;
		P2 = 0xfe; //初始化P0端口，由开发板得知，低电平灯亮
		delay(100);
		for(i=0;i<8;i++) // 循环一次，所亮的灯移动一位
		{
			P2 = (P2<<1)+1; // 移位加一
			delay(100);
		}
	}
}

